# timer_apiserver/database

This directory holds marterials related to database(s) of this project(e.g. migration files).

Folder `migrations` contains migration files created and manged by [go-migrate cli](https://github.com/golang-migrate/migrate/tree/master/cmd/migrate)
